"""Native tool modules invoked directly by the dialekt server.

Distinct from ``dialekt.mcp.*`` — these are first-party integrations
(Instagram publisher, etc.) that don't go through the MCP runtime
because they need bespoke OAuth flows or token-refresh schedules.
"""
